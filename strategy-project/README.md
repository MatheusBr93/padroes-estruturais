
```bash
mvn compile exec:java -Dexec.mainClass=strategy.app.Main
```

Ou, pela raiz do projeto multi-módulo:

```bash
mvn -pl strategy-project compile exec:java -Dexec.mainClass=strategy.app.Main
```
mvn -pl strategy-project compile exec:java -Dexec.mainClass=strategy.app.Main