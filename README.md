# Execução dos Projetos de Padrões de Projeto

- Via Maven utilizando os comandos abaixo

---

# State

```bash
mvn -pl state-project compile exec:java -Dexec.mainClass=state.app.Main
```

---

# Observer

```bash
mvn -pl observer-project compile exec:java -Dexec.mainClass=observer.app.Main
```

---

# Strategy

```bash
mvn -pl strategy-project compile exec:java -Dexec.mainClass=strategy.app.Main
```

---

# Decorator

```bash
mvn -pl decorator-project compile exec:java -Dexec.mainClass=decorator.app.Main
```

---

# Chain of Responsibility

```bash
mvn -pl chain-of-responsibility-project compile exec:java -Dexec.mainClass=chain.app.Main
```

---

# Builder

```bash
mvn -pl builder-project compile exec:java -Dexec.mainClass=builder.app.Main
```

---

# Mediator

```bash
mvn -pl mediator-project compile exec:java -Dexec.mainClass=mediator.app.Main
```

---

# Template Method

```bash
mvn -pl template-method-project compile exec:java -Dexec.mainClass=template.app.Main
```

---

# Composite

```bash
mvn -pl composite-project compile exec:java -Dexec.mainClass=composite.app.Main
```

---

# Facade

```bash
mvn -pl facade-project compile exec:java -Dexec.mainClass=facade.app.Main
```

---

# Memento

```bash
mvn -pl memento-project compile exec:java -Dexec.mainClass=memento.app.Main
```

---

# Visitor

```bash
mvn -pl visitor-project compile exec:java -Dexec.mainClass=visitor.app.Main
```

---

# Flyweight

```bash
mvn -pl flyweight-project compile exec:java -Dexec.mainClass=flyweight.app.Main
```

---

# Iterator

```bash
mvn -pl iterator-project compile exec:java -Dexec.mainClass=iterator.app.Main
```

---

# Prototype

```bash
mvn -pl prototype-project compile exec:java -Dexec.mainClass=prototype.app.Main
```

---

# Adapter

```bash
mvn -pl adapter-project compile exec:java -Dexec.mainClass=adapter.app.Main
```

---

# Command

```bash
mvn -pl command-project compile exec:java -Dexec.mainClass=command.app.Main
```

---

# Proxy

```bash
mvn -pl proxy-project compile exec:java -Dexec.mainClass=proxy.app.Main
```

---

# Interpreter

```bash
mvn -pl interpreter-project compile exec:java -Dexec.mainClass=interpreter.app.Main
```
