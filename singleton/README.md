# Singleton Project

Projeto simples em Java para demonstrar o padrão Singleton.

## Estrutura do padrão
- Singleton: `Logger`
- Classe cliente: `Main`

## Como executar
Compile os arquivos da pasta `src` e execute `app.Main`.
